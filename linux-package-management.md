     clear
     apt
     sudo apt install nload
     nload -h
     sudo apt remove nload
     nload -h
     sudo apt install tcpdump
     aptlist --installed
     apt list --installed
     sudo apt install nload
     apt list --installed
     history
